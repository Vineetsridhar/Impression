import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        display: 'flex',
        flex: 1,
        alignItems: 'center'
    },
    avatarStyle: {
        height: 200,
        width: 200,
        marginTop: 50
    },
    loginbutton: {
        width: 200,
        resizeMode: 'contain'
    }

})

export default styles